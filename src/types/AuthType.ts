export interface EnterUserType {
  classHid?: string;
  children: React.ReactNode;
}

export interface LoginFormData {
  email: string;
  password: string;
}

export interface SignupFormData {
  username: string;
  email: string;
  password: string;
}